# All values come from /opt/config/project/.env.all (via op run or manual copy),
# except the two host overrides below which must point to localhost when running
# outside Docker (the .env.all values use Docker-internal hostnames).
ANALYTICS_DB_HOST=localhost
MINIO_ENDPOINT=http://localhost:9000
