name: "analytics"
version: "1.0.0"
config-version: 2

profile: "analytics_marts"

model-paths: [ "models" ]

models:
  analytics:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
